- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.zshenv: .zshenv
    ~/.gitconfig: .gitconfig
    ~/.config/starship.toml: .config/starship.toml
    ~/.mambarc: .mambarc
    ~/.hushlogin: .hushlogin
    ~/.zsh_aliases/.default:
      create: true
      path: .zsh_aliases/.default
    ~/.config/nvim/lua/user:
      create: true
      path: .config/nvim/lua/user

- shell:
    - command: ./install/.homebrew
      stdout: true
      stderr: true
    - command: ./install/.zsh
      stdout: true
      stderr: true
    - command: ./install/.node
      stdout: true
      stderr: true
    - command: ./install/.macos
      stdout: true
      stderr: true


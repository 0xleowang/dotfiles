- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.gitconfig: .gitconfig

- create:
    - ~/Work

- shell:
    -
      command: ./install_homebrew.zsh
      stdout: true
      stderr: true


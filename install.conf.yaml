- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.gitconfig: .gitconfig

- shell:
    -
      command: ./install_homebrew.zsh
      stdout: true
      stderr: true


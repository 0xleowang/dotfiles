- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.gitconfig: .gitconfig
    ~/.config/nvim/lua/user:
      create: true
      path: .config/nvim/lua/user

- shell:
    -
      command: ./install_homebrew.zsh
      stdout: true
      stderr: true

